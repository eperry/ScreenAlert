# Example secrets file for act
# Copy this to .secrets and fill in your values
# Do NOT commit .secrets to git (it's in .gitignore)

SIGNING_CERTIFICATE=base64_encoded_certificate_here
SIGNING_PASSWORD=your_certificate_password_here
